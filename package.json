{
  "name": "nuxt-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.0",
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "nuxt": "^2.14.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
